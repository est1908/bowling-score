import { ScoreTable } from './score-table';

test('first frame; first try', () => {
    // const table = createTable();
    // table.add()
    // expect(1).toBe(1);
});

function createTable() {
    return new ScoreTable();
}
